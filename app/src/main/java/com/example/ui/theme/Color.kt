package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated Dark Theme Palette
val SophisticatedBackground = Color(0xFF0F1113)      // Sleek Obsidian Charcoal
val SophisticatedSurface = Color(0xFF1B2025)         // Dark Charcoal Panel
val SophisticatedSurfaceVariant = Color(0xFF1F2429)  // Lighter Slate Grey Panel
val SophisticatedBorder = Color(0xFF30363D)          // Muted border

val SophisticatedAccentBlue = Color(0xFF70D2FF)      // Vibrant Sky/Light Blue
val SophisticatedElectricBlue = Color(0xFF3B82F6)    // Premium Electric Blue
val SophisticatedAccentTertiary = Color(0xFFD1E4FF)  // Pale Contrast Accent

val SophisticatedText = Color(0xFFE1E2E5)            // Crisp off-white text
val SophisticatedMutedText = Color(0xFF8E9196)       // Neutral grey secondary text

// Retain legacy names mapping to maintain backwards compatibility if used elsewhere
val SonicPrimary = SophisticatedAccentBlue
val SonicSecondary = SophisticatedElectricBlue
val SonicTertiary = SophisticatedAccentTertiary

val BackgroundDark = SophisticatedBackground
val SurfaceDark = SophisticatedSurface
val SurfaceVariantDark = SophisticatedSurfaceVariant

val TextPrimaryDark = SophisticatedText
val TextSecondaryDark = SophisticatedMutedText
val TextMutedDark = SophisticatedMutedText

